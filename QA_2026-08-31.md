# Poco a Poco — MVP audit / 2026-08-31

## Scope

公開済みMVPの学習フロー、教材整合、Japanese Support、モバイル向けCSS、公開運用を監査。

対象フロー:

HOME → 今日の5分 → SCENE → MEANING / NOTICE → CHUNK → RETRIEVE → SPEAK → CHANGE → PERSONALIZE → REUSE → DONE → PRACTICE

## P0 — fixed

### RETRIEVEの問いと答えが一致していなかった

7レッスンで、RETRIEVEは変化させた文を要求しているのに、答え表示が元の例文を表示していた。

例:

- 問い: 「私はコーヒーが好きです」
- 旧答え: `Me gusta el fútbol.`
- 修正後: `Me gusta el café.`

全8レッスンに `retrieveAnswer` / `retrieveAnswerJa` / `retrieveCueEs` を追加し、RETRIEVEとSPEAKで同じ正答を使うよう修正。

## P1 — fixed

### SPEAKが実際には文を隠していなかった

旧UIは LOOK → HIDE → SAY と案内していたが、文は常時表示されていた。

修正後:

1. 文を見る
2. 「文を隠して話す」
3. 文をマスク
4. 声に出す
5. 必要なら見直す
6. 次へ

の実操作へ変更。

### PERSONALIZEを空欄で通過できた

旧実装では空欄でも `...` を含むテンプレートを保存して完了できた。

修正後は空欄では進まず、エラーを表示。CHANGEで選んだ例文を下書きとして使う補助導線を追加。

### Japanese Support Level 1〜4の差が弱かった

修正後:

- Level 1: 日本語中心。Step名にも日本語併記
- Level 2: スペイン語 + 日本語を同時表示
- Level 3: スペイン語中心。日本語はdetailsを開いて確認
- Level 4: レッスン内は原則スペイン語

SCENE / NOTICE / RETRIEVE / REUSE用のスペイン語補助文を教材データへ追加。

### 完了画面でPersonalize文と日本語訳が不一致になる可能性

自分で作ったスペイン語文を表示しながら元例文の日本語訳を出す構造を廃止。完了画面は「今日の一文」を主役にする。

### iPhone互換性リスク

`window.scrollTo({ behavior: 'instant' })` を標準的な `behavior: 'auto'` へ変更。

### UI上でPATH / PRACTICE / DISCOVERYの差が弱かった

- PATH: 縦レール + green accent
- PRACTICE: orange accent + flatter cards
- DISCOVERY: yellow accent + editorial 2-column layout on wide screens

へ分離。

### モバイル操作

- safe areaを含む下余白
- 44〜48px以上の主要タップ領域
- mobileでlesson actionを1列化
- dialogのmax-height / scroll
- textarea 16px
- focus-visible

を追加。

## Content fixes

- `poco a poco` の見出しを「まあ、ぼちぼち」から「少しずつ」へ修正。
- `Estoy ocupado.` の性による形の変化を初心者MVPで避け、CHANGE例を `Estoy aquí.` に変更。

## Automated smoke tests

Node上のDOM stubで以下を確認:

- HOMEがrenderされる
- 全8レッスンでRETRIEVE正答が対応する
- SPEAK hidden stateで正答文がDOMから消える
- PERSONALIZE空欄ではstepが進まない
- Level 3はSpanish-first + Japanese optional help
- Level 4はSpanish lesson title / sceneとなり日本語sceneが出ない
- `behavior:'instant'` が残っていない
- JavaScript syntax check PASS
- CSS brace balance PASS

## Remaining limitations

### P1 candidate

Personalizeした自由入力のスペイン語自体の正誤判定はしない。MVP原則（AI / speech recognitionなし）を維持しているため、現時点では意図的な制約。

### P2

- Lesson数は8で、A0→A1の体系としてはまだ浅い
- Smart Randomは説明可能なheuristicだが、interval-based spaced repetitionではない
- 学習履歴はlocalStorageのみで端末間同期しない
- DISCOVERYは固定6件

## Recommended next priority

NOW:

1. 現行8レッスンを数日使って、離脱step / 面倒なstepを記録
2. Can-do PATHを20〜30件へ設計してから、Lesson追加を小分けに実装
3. PRACTICEの復習タイミングを `confidence × elapsed days × repetitions` から明示的なdue date方式へ発展

NEXT:

- Personalize履歴一覧
- 5 / 15 / 30分のsession composition
- Discovery拡張

LATER:

- 端末間同期

NOT NOW:

- AI tutor
- AI conversation
- speech recognition
- pronunciation scoring
- ranking / SNS / complex gamification

## Environment note

この監査環境では実ブラウザのiPhone Safariを直接操作できないため、公開環境の最終視覚確認は自動DOM / CSS監査とGitHub Pages deploy検証で代替している。実機Safariでの最終visual acceptanceは残タスク。
